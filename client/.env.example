# ── API ───────────────────────────────────────────────────────────────────────
# Base URL of the FastAPI backend.
# Development: leave empty — Vite proxies /api/* to localhost:8000 automatically.
# Production:  set to the full backend URL (e.g. https://your-railway-app.up.railway.app).
VITE_API_BASE_URL=
